export enum Action {
    Manage = "manage",
    Create = "create",
    Read = "read",
    Update = "update",
    Delete = "delete",
}

export enum Subject {
    Users = "users",
    Orders = "orders",
    Items = "items",
    All = "all",
}
